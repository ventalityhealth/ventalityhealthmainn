<section class="vt-container" style="padding: clamp(80px, 10vw, 120px) 0;">
  <div style="max-width: 440px; margin: 0 auto;">
    <h1 class="vt-heading-lg vt-mb-8 vt-text-center">Reset your password</h1>
    <div class="vt-panel vt-panel-pad-lg">
      {%- form 'reset_customer_password' -%}
        {%- if form.errors -%}
          <div class="vt-mb-4" style="color: var(--vt-warm);">{{ form.errors | default_errors }}</div>
        {%- endif -%}
        <div style="display:flex;flex-direction:column;gap:12px;">
          <input type="password" name="customer[password]" class="vt-input" placeholder="New password" required>
          <input type="password" name="customer[password_confirmation]" class="vt-input" placeholder="Confirm new password" required>
          <button type="submit" class="vt-button-primary vt-mt-4">Reset password</button>
        </div>
      {%- endform -%}
    </div>
  </div>
</section>
